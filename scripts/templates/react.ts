import React from "react";
import { createComponent } from "@lit/react";
import { {{{ELEMENT_CLASS}}} } from "{{{ELEMENT_IMPORT}}}";

export default createComponent({
  react: React,
  tagName: "{{{TAG_NAME}}}",
  elementClass: {{{ELEMENT_CLASS}}},
});
